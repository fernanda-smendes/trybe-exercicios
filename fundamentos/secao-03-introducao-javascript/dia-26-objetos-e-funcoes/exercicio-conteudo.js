
let a = 2;
let b = 5;

// adição

function soma (a, b) {
    return a + b;
}

console.log(soma(a, b));


// Subtração

function sub (a, b) {
    return a - b;
}

console.log(sub(a, b));

// Multiplicação

function mult (a, b) {
    return a * b;
}

console.log(mult(a, b)); 

// Divisão

function div (a, b) {
    return a / b;
}

console.log(div(a, b)); 

function mod (a, b) {
    return a % b;
}

console.log(mod(a, b));