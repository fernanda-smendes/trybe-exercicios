
let a = 2;
let b = 5;

// primeiro passo: Faça cinco programas, um para cada operação aritmética básica. Seu programa deve ter duas constantes, a e b, definidas no começo com os valores que serão operados. 

// adição

/* function soma (a, b) {
    return a + b;
}

console.log(soma(a, b));


// Subtração

function sub (a, b) {
    return a - b;
}

console.log(sub(a, b));

// Multiplicação

function mult (a, b) {
    return a * b;
}

console.log(mult(a, b)); 

// Divisão

function div (a, b) {
    return a / b;
}

console.log(div(a, b)); 

function mod (a, b) {
    return a % b;
}

console.log(mod(a, b)); */

// segundo passo: Faça um programa que retorne o maior de dois números. Defina, no começo do programa, duas constantes com os valores que serão comparados.

function compar(a, b) {
    if (a > b) {
        console.log(a + " é maior que " + b);
    } else {
        console.log(b + " é maior que " + a);
    }
}


